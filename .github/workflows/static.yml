name: Deploy Main Website to Pages

on:
  push:
    branches: ["cutting-edge"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          static_site_generator: ''
          path: 'main-website'

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: 'main-website'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4